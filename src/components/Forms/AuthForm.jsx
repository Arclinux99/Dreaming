'use client'
import React from 'react'

const AuthForm = () => {
  return (
    <div>
        <form action={}/>
        <button className='btn' formAction={SignInWithGoogle}>Sign In with Google</button>
    </div>
  )
}
